"""jobhunter-agent: autonomous job application pipeline.

Inspired by a video demo in which an agent scrapes jobs, hard-filters them,
scores them by direction, tailors a one-page resume per job, fills the
application form in a real browser, passes seven pre-submission gates,
and only then submits. Humans only: decide, register accounts, hand over
SMS codes. Everything else is automated.
"""

__version__ = "0.1.0"
